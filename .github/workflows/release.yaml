name: Release

on:
  push:
    tags:
      - "v*"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Build binaries
        run: |
          GOOS=linux GOARCH=amd64 go build -ldflags="-s -w" -o aegisflow-linux-amd64 ./cmd/aegisflow
          GOOS=linux GOARCH=arm64 go build -ldflags="-s -w" -o aegisflow-linux-arm64 ./cmd/aegisflow
          GOOS=darwin GOARCH=arm64 go build -ldflags="-s -w" -o aegisflow-darwin-arm64 ./cmd/aegisflow
          GOOS=darwin GOARCH=amd64 go build -ldflags="-s -w" -o aegisflow-darwin-amd64 ./cmd/aegisflow

      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          files: |
            aegisflow-linux-amd64
            aegisflow-linux-arm64
            aegisflow-darwin-arm64
            aegisflow-darwin-amd64
          generate_release_notes: true
